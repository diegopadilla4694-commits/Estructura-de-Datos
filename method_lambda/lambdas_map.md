# Lambda Method and Map() Method

With this method, I learned that a simple list "x = [ ]" like this one can be used with the lambda function.
This method takes a variable x and raises it to the power of 2. Map applies this function to each element.

""In the program I uploaded, it's applied to list_1"

- In my program, it initially creates a single list: [0, 1, 2, 3, 4]. Then, it uses the lambda function,
taking each value and squaring it: [1, 2, 4, 8, 16].

- At the end of the program, a for loop using the map() method is combined with a lambda function. This function
takes each value from list_2 and multiplies it by itself, resulting in 1, 4, 16, 64, 256 at the end of the loop,
giving this as the mathematical result.


